import React from "react";
import { Tilt } from "react-tilt"; // If Tilt is a named export
import { motion } from "framer-motion";
import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img src={icon} alt={title} className="w-16 h-16 object-contain" />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-[#d6d3de] text-[17px] max-w-3xl leading-[30px]"
      >
        I'm a passionate web developer with expertise in HTML, CSS, JavaScript,
        and frameworks like React and Node.js. I specialize in creating
        user-focused, responsive web applications and have a strong foundation
        in digital marketing. I'm a fast learner who thrives on collaborating
        with clients to deliver innovative, scalable solutions. Let's work together to bring your ideas to life!

      </motion.p>

      {/* Apply the 2-column grid here */}
      <div className="mt-20 grid grid-cols-1 md:grid-cols-3 gap-10 right-5">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
